{
  "name": "2n8",
  "version": "0.0.1",
  "description": "Simple state management.",
  "license": "ISC",
  "author": "Will Stone",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "prepare": "simple-git-hooks",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "simple-git-hooks": {
    "pre-commit": "npx --no lint-staged"
  },
  "lint-staged": {
    "*.{js,ts}": [
      "eslint --fix"
    ],
    "*.{css,json,md,js,ts}": [
      "prettier --write"
    ]
  },
  "prettier": "@will-stone/prettier-config",
  "devDependencies": {
    "@will-stone/eslint-config": "^12.0.2",
    "@will-stone/prettier-config": "^8.0.1",
    "eslint": "^9.16.0",
    "lint-staged": "^15.2.10",
    "prettier": "^3.4.1",
    "simple-git-hooks": "^2.11.1"
  },
  "overrides": {
    "eslint": "$eslint"
  }
}
